name: syncache_flutter_example
description: Example app demonstrating syncache_flutter usage.
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.0.0

dependencies:
  flutter:
    sdk: flutter
  syncache:
    path: ../../syncache
  syncache_flutter:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
